---
title: Site Index
filter:
- erb
- textile
---
h2. <%= h(@page.title) %>

<%
  # 
  articles = @pages.find(:all, :in_directory => @path,
                         :sort_by => "name", :reverse => false)
  paginate(articles, 10) do |page|
%>
<%= page.render %>
<hr />
<% end %>

<%= link_to("Prev", @pager.prev) if @pager.prev? %>
<%= link_to("Next", @pager.next) if @pager.next? %>